version: '3.7'

services:
  web:
    build: .
    user: 1000:1000
    command: python /code/manage.py runserver 0.0.0.0:8000
    environment:
      - SECRET_KEY=django-insecure-mr-yra=(99#b35roj)za%h23t(l*7o$lzk!-9ktc8nfnp+^^ju
      - DEBUG=1
    volumes:
      - .:/code
    ports:
      - 8000:8000
#     depends_on:
#       - db

#   db:
#     image: postgres:11
#     volumes:
#       - postgres_data:/var/lib/postgresql/data/
#     environment:
#       - "POSTGRES_HOST_AUTH_METHOD=trust"

# volumes:
#   postgres_data:
